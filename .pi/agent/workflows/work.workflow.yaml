version: 1
id: work
command: work
description: "Prepare a dedicated workspace, plan, implement, and verify local work. Example: /work Add rate limiting to user registration endpoint"
start: prepare-workspace
maxStepVisits: 20
summaryMaxChars: 30000
steps:
  prepare-workspace:
    title: Prepare dedicated branch and worktree
    prompt:
      file: steps/shared/prepare-workspace.md
    agent: workspace-preparer
    workspace:
      bindOn:
        - ready
      allowedRoots:
        - ~/repositories/worktrees
    permissions:
      tools: [read, ls, bash]
      mcp: []
      extensions: [/]
      skills: [using-superpowers, using-git-worktrees, caveman]
      bash:
        mode: unrestricted
    transitions:
      ready: plan
      retry: prepare-workspace
      blocked: $pause
  plan:
    title: Explore and plan local work
    prompt:
      file: steps/work/plan.md
    agent: planner
    permissions:
      tools: [read, ls, bash, mcp]
      mcp: [context7, sourcegraph, glean, gh_grep, github/get_file_contents, gitlab]
      extensions: [/]
      skills: [using-superpowers, caveman, ponytail]
      bash:
        mode: unrestricted
    gate:
      provider: plannotator
      submitOutcome: submit
      approvedOutcome: approved
      rejectedOutcome: changes-requested
      timeoutMs: 30000
      artifactContract:
        maxChars: 8000
        requiredSubstrings:
          - "## Review summary"
          - "## Review focus"
          - "## Proposed approach"
          - "## Validation"
          - "## Risks"
          - "## Execution appendix (machine-readable)"
          - "## Publication contract"
        forbiddenSubstrings:
          - "The complete gate artifact is the exact Markdown saved at"
        onValidationFailure: retry
    transitions:
      approved: implement
      changes-requested: plan
      workspace-refresh: prepare-workspace
      retry: plan
      blocked: $pause
  implement:
    title: Implement the approved local plan
    prompt:
      file: steps/work/implement.md
    agent: worker
    permissions:
      tools: [read, ls, bash, edit, write, mcp]
      mcp: [context7]
      extensions: [/]
      skills: [using-superpowers, caveman, test-driven-development, systematic-debugging]
      bash:
        mode: unrestricted
    transitions:
      ready: verify
      retry: implement
      blocked: $pause
  verify:
    title: Independently verify local work
    prompt:
      file: steps/work/verify.md
    agent: reviewer
    permissions:
      tools: [read, ls, bash]
      mcp: []
      extensions: [/]
      skills: [using-superpowers, caveman, verification-before-completion, caveman-review, ponytail-review]
      bash:
        mode: unrestricted
    transitions:
      passed: publish
      failed: implement
      retry: verify
      blocked: verify
  publish:
    title: Publish verified branch and open review
    prompt:
      file: steps/shared/publish-remote.md
    agent: worker
    permissions:
      tools: [read, ls, bash, mcp]
      mcp: [gitlab, github/pull_request_read, github/create_pull_request]
      extensions: [/]
      skills: [using-superpowers, caveman, verification-before-completion]
      bash:
        mode: unrestricted
    transitions:
      published: $done
      retry: publish
      blocked: $pause
